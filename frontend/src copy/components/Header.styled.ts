import { Container } from '@mui/material';
import styled from 'styled-components';

const HeaderContainer = styled(Container)`
  display: flex;
`;
const HeaderLogoImg = styled.img`
  width: 70px;
  height: 70px;
`;

export { HeaderContainer, HeaderLogoImg };
